.env
node_modules/

# runtime artifacts (regenerated every run)
tweets.json
digest.html
market.json
*.log
fetch.err

# one-off discovery artifacts
hl-candidates.json
caller-candidates.json

# local position baseline — the cloud workflow owns/commits this (git add -f)
state/hl-positions.json

# cloudflare worker
bot/node_modules/
bot/.wrangler/
